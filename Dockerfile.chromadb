# Custom ChromaDB image with curl for healthcheck
FROM chromadb/chroma:latest

# Install curl for healthcheck
RUN apt-get update && \
    apt-get install -y curl && \
    apt-get clean && \
    rm -rf /var/lib/apt/lists/*

# Everything else is inherited from the base image